import React from "react";
import OrionTransLogo from '../assets/logo.png';

const MatxLogo = ({className}) => {
   return (
       <img className={className} src={OrionTransLogo} alt="logo"/>
   );
};

export default MatxLogo;
